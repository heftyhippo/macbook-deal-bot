# ============================================================
#  Apple Deal Bot - configuration
#  Main settings live here. Local secrets live in config.local.yaml.
#  Lines starting with # are comments and are ignored.
# ============================================================

# WhatsApp alerts via CallMeBot (free, one-time setup - see README step C):
# add +34 644 53 78 49 to your phone contacts, WhatsApp it the message
# "I allow callmebot to send me messages", then put the returned key and your
# phone number in the ignored config.local.yaml (see README step C).
whatsapp:
  enabled: false         # local credentials belong in ignored config.local.yaml
  phone: ""              # never commit a real phone number
  apikey: ""             # never commit a real API key

# ------------------------------------------------------------------
# Listings share one ranking but retain clear condition labels. This section
# controls the like-new tier's battery-cycle ceiling.
# ------------------------------------------------------------------
personal:
  enabled: true
  # Battery cycles you accept for a "practically new" personal machine
  # (only enforced when a cycle count is actually stated/found).
  max_battery_cycles: 60

# ------------------------------------------------------------------
# UNIFIED DEAL RANKING: every buyable listing is compared with the expected
# UK price for the same model, specification and condition. New, like-new and
# sound used stock therefore share one honest ranking instead of competing
# "profit", "saving" and "best value" sections.
# ------------------------------------------------------------------
value:
  # One condition-aware ranking: new, like-new and sound used listings are
  # all compared with the price a UK buyer would expect for THAT condition.
  enabled: true
  top_n: 250
  # Fair-value fallbacks when a model has no eBay-UK USED sold median yet
  # (run `python macdeals.py ukprices --write` to fill those in with real
  # data): like-new units trade ~88% of new, good-condition ~78%.
  like_new_factor: 0.88
  good_factor: 0.78
  # Battery economics: sold prices already include normal wear for each
  # condition, so only cycles ABOVE that tier's baseline lower the expected
  # price. Apple rates these batteries at 1000 cycles; service is ~£249.
  battery_replacement_gbp: 249
  battery_cycle_rating: 1000
  # Hard floor: listings above this cycle count are heavily used machines
  # (and that much use is itself a poor-condition signal) - never shown.
  max_battery_cycles: 800
  # A listing priced below this fraction of its condition-fair value is
  # scam/damage/mislist territory and is kept out of the ranking. 0.45 =
  # anything cheaper than 45% of what that condition normally costs.
  implausible_value_ratio: 0.45
  # Spec-aware benchmarks: listings above/below the model's base_spec get
  # their UK benchmark adjusted so a 48GB/2TB machine isn't scored against
  # base-model money (and vice versa). GBP per step, tune freely.
  spec_adjustments:
    ram_per_8gb_gbp: 60        # each 8GB of RAM above the base spec
    ssd_gbp: {128: -150, 256: -80, 512: 0, 1024: 130, 2048: 350,
              4096: 700, 8192: 1200, 16384: 2000}

# How the one expected UK price is derived after the model/base-spec match.
# The dashboard always compares the all-in landed cost with this number.
expected_price:
  configured_benchmark_confidence: 0.72
  open_box_factor: 0.94
  ambiguous_new_factor: 0.97
  like_new_new_weight: 0.50
  keyboard_penalty_gbp: {JIS: 120, EU: 80, unknown_jp: 100}
  imac_keyboard_penalty_gbp: 80
  studio_display_nano_premium_gbp: 150
  studio_display_height_premium_gbp: 150

alerts:
  # One threshold everywhere. Foreign keyboard/layout disadvantages are now
  # deducted from expected price directly instead of hidden in regional bars.
  min_savings_pct: 35
  hot_savings_pct: 40
  too_good_pct: 55
  min_listing_confidence: 0.55
  min_benchmark_confidence: 0.50
  # `hot` changes the message headline. `too_good` marks a result for manual
  # verification and blocks alerts; it remains visible as a lead.
  # Below this ratio the item is suspiciously cheap relative to the expected
  # price. It stays visible at reduced confidence but cannot alert.
  implausible_price_ratio: 0.45
  # Below this lower hard floor the result is almost certainly an accessory,
  # component or scam and is excluded. Between the two floors it stays visible
  # as a low-confidence lead but can never alert.
  hard_exclude_price_ratio: 0.35
  # Max battery charge cycles you accept for "opened but unused" machines.
  # (Only enforced when the bot can actually find a cycle count in the listing text.)
  max_battery_cycles: 10
  # Re-alert on an already-alerted item only if its price drops by this % or more.
  realert_drop_pct: 5

# ------------------------------------------------------------------
# Optional resale-profit estimate retained in CSV/backward-compatible fields.
# The main dashboard ranking is always expected-price saving, not profit.
# ------------------------------------------------------------------
resale:
  sell_friction_pct: 5

costs:
  # Proxy-service fee per item, in yen.
  # ZenMarket: 800 yen for Mercari / auction items (2026). Buyee: 300-500 yen.
  # 800 is used as the safe default so estimates err on the expensive side.
  proxy_fee_jpy: 800
  # Domestic shipping inside Japan (seller -> proxy warehouse).
  # Mercari listings nearly always include domestic shipping; Yahoo often doesn't.
  domestic_shipping_jpy:
    mercari: 0
    yahoo: 1200
    rakuma: 0
  # International courier (DHL/FedEx/UPS) for a boxed 14"/16" laptop, ~2-4 kg.
  intl_shipping_jpy: 8000
  # ...and per product family (a Mac mini posts cheaply; a 27" glass display or an
  # iMac needs serious packing). Families not listed use intl_shipping_jpy.
  intl_shipping_jpy_family:
    mac_mini: 6000
    mac_studio: 12000
    imac: 20000
    mac_pro: 30000
    display: 25000

  # ---- US route (eBay US) ------------------------------------------------
  # US sales tax on the purchase. 0 is right when the seller ships
  # internationally / via eBay International Shipping (exports aren't taxed),
  # or when your US forwarder address is in a no-sales-tax state
  # (Oregon, Delaware, Montana, New Hampshire). If your forwarder is in a
  # taxed state (e.g. Florida ~7), put that percentage here.
  us_sales_tax_pct: 0
  # Package-forwarder processing fee per shipment (MyUS / Shipito style).
  # Buying directly with eBay International Shipping skips this fee - the
  # estimate stays deliberately a little pessimistic.
  us_forwarder_fee_usd: 12
  # Seller -> forwarder leg inside the US. Many eBay listings ship free;
  # a small buffer keeps the estimate honest.
  us_domestic_shipping_usd: 10
  # Forwarder -> UK express courier for a boxed 14"/16" laptop, ~2-4 kg.
  us_intl_shipping_usd: 85
  # ...and per product family (same idea as the JP table above).
  us_intl_shipping_usd_family:
    mac_mini: 60
    mac_studio: 110
    imac: 180
    mac_pro: 250
    display: 200

  # ---- EU route (eBay Germany) --------------------------------------------
  # Most DE sellers ship EU-wide and many post to the UK; where they don't,
  # an EU parcel forwarder adds ~€10 (folded into these estimates). UK import
  # VAT applies just like the JP/US routes (0% duty on computers/tablets).
  eu_shipping_eur: 30
  eu_shipping_eur_family:
    mac_mini: 25
    mac_studio: 45
    imac: 70
    mac_pro: 90
    display: 80

  # UK import charges. Laptops are 0% customs duty, but 20% import VAT applies
  # to (item + shipping), plus a courier clearance/handling fee.
  apply_uk_import: true
  uk_vat_pct: 20
  courier_handling_gbp: 12

  # ---- UK route (eBay UK) --------------------------------------------------
  # Domestic purchases have no import costs at all - just a small postage
  # buffer (many listings ship free).
  uk_domestic_shipping_gbp: 8

fx:
  # Live JPY/USD/EUR-per-GBP rates are fetched automatically
  # (frankfurter.dev, ECB data). These values are only used if the fetch fails.
  # (Checked July 2026: ~216 JPY, ~1.33 USD, ~1.17 EUR per GBP.)
  fallback_jpy_per_gbp: 216
  fallback_usd_per_gbp: 1.33
  fallback_eur_per_gbp: 1.17

scan:
  # Marketplaces to scan. JP sources are bought through Buyee (and ZenMarket
  # where it proxies them); eBay US/DE items are bought directly or through
  # a package forwarder.
  #   mercari    = Mercari JP (huge C2C flea market, via official app API)
  #   yahoo      = Yahoo! Auctions JP (via Buyee - Japan's biggest auctions)
  #   rakuma     = Rakuten Rakuma (via Buyee - 2nd-biggest C2C flea market)
  #   paypay     = PayPay Flea Market (via Buyee - Yahoo's fixed-price app)
  #   ebay_us    = eBay US (the biggest US resale market)
  #   swappa     = Swappa (US, human-verified. Needs Google Chrome +
  #                `pip install patchright`; home connections only)
  #   ebay_uk    = eBay UK (domestic - no import costs, UK returns)
  #   gumtree    = Gumtree UK classifieds (domestic; often collection-only,
  #                no buyer protection - meet locally, pay on collection)
  #   ebay_de    = eBay Germany (biggest EU market; EUR prices, UK VAT on
  #                import applies; QWERTZ keyboards on MacBooks/iMacs)
  # Remove any you don't want; add back any time.
  sources: [mercari, yahoo, rakuma, paypay, ebay_us, swappa,
            ebay_uk, gumtree, ebay_de]
  # Search queries (sent to every market - JP sellers list product names in
  # romaji, so the same phrases work everywhere). `family` picks the right
  # price floor and shipping estimates for what the query is hunting.
  queries:
    - {q: "MacBook Pro M2", family: macbook}
    - {q: "MacBook Pro M3", family: macbook}
    - {q: "MacBook Pro M4", family: macbook}
    - {q: "MacBook Pro M5", family: macbook}
    - {q: "Mac Studio", family: mac_studio}
    - {q: "Mac mini M2", family: mac_mini}
    - {q: "Mac mini M4", family: mac_mini}
    - {q: "iMac M3", family: imac}
    - {q: "iMac M4", family: imac}
    - {q: "Mac Pro M2 Ultra", family: mac_pro}
    - {q: "Apple Studio Display", family: display}
  # Mercari condition IDs to accept: 1 = brand new/unused; 2 = nearly unused
  # and 3 = "no visible scratches or dirt" (both LIKE-NEW); 4 = some light
  # wear (USED). Every tier is compared with its own expected price.
  mercari_conditions: [1, 2, 3, 4]
  # Pages of Mercari results per query (about 120 items per page, newest first).
  mercari_pages: 2
  # Gumtree is covered with two category-scoped, newest-first feeds. The
  # remaining request budget follows later pages without a burst of searches.
  gumtree_request_budget: 4
  gumtree_pages_per_feed: 2
  gumtree_request_delay_seconds: 3
  gumtree_rate_limit_backoff_seconds: 15
  gumtree_feeds:
    - {name: "Apple Macs", url: "https://www.gumtree.com/for-sale/computers-software/computers-pcs-laptops/macs?sort=date"}
    - {name: "Studio Display", url: "https://www.gumtree.com/search?search_category=computer-monitors&search_location=uk&q=Apple+Studio+Display&sort=date"}
  # Extra URL parameters for Yahoo Auctions search.
  # istatus=2 = "unused" condition filter, n=50 results, s1=new&o1=d = newest first.
  # If Yahoo ever changes things: do the search in your browser with the filters
  # you want, and copy everything after the "?" from the address bar into here.
  # Yahoo Auctions is reached VIA BUYEE: Yahoo! JAPAN geo-blocks all UK/EEA
  # visitors, but Buyee mirrors its full catalogue and works from the UK.
  buyee_extra_params: "translationType=98&istatus=2"
  yahoo_extra_params: "istatus=2&n=50&s1=new&o1=d"   # legacy (direct access from outside UK/EEA)
  # Rakuten Rakuma is also reached via Buyee. status=on_sale excludes SOLD
  # items (verified July 2026: status=all mixes sold-out listings into the
  # results); the bot still insists the title says new/unused (same as Yahoo),
  # and also skips any card carrying Buyee's "soldOut" overlay as a backstop.
  rakuma_extra_params: "status=on_sale&order=desc&sort=created_time"
  # Extra URL parameters for the eBay US search.
  # LH_ItemCondition=1000|1500 = Brand New + Open box, LH_BIN=1 = Buy It Now
  # only (drop it to include auctions), LH_PrefLoc=1 = US-located items,
  # _sop=10 = newest first, _ipg=60 = 60 results per query.
  ebay_us_extra_params: "LH_ItemCondition=1000%7C1500&LH_BIN=1&LH_PrefLoc=1&_sop=10&_ipg=60"
  # Second eBay pass for the practically-new tier: condition Used (3000),
  # kept only when the title itself claims like-new/mint/pristine/low cycles.
  ebay_us_personal_extra_params: "LH_ItemCondition=3000&LH_BIN=1&LH_PrefLoc=1&_sop=10&_ipg=60"
  # Swappa condition grades to accept. "New" = sealed, "Mint" = like-new,
  # "Good" = light wear. Each is valued against its own expected price;
  # Swappa human-reviews every listing.
  swappa_conditions: ["New", "Mint", "Good"]
  # eBay UK search params (same format as the US ones above).
  ebay_uk_extra_params: "LH_ItemCondition=1000%7C1500&LH_BIN=1&LH_PrefLoc=1&_sop=10&_ipg=60"
  ebay_uk_personal_extra_params: "LH_ItemCondition=3000&LH_BIN=1&LH_PrefLoc=1&_sop=10&_ipg=60"
  # Ignore listings below these prices (filters out boxes, stickers, pencils,
  # stands, chargers, junk). Per family because a near-new Mac mini M2 costs
  # a fraction of a Mac Studio. Queries use the floor of their `family`.
  min_price:
    macbook:    {jpy: 45000,  usd: 300,  gbp: 250,  eur: 300}
    mac_mini:   {jpy: 25000,  usd: 180,  gbp: 150,  eur: 180}
    mac_studio: {jpy: 60000,  usd: 450,  gbp: 350,  eur: 450}
    imac:       {jpy: 50000,  usd: 380,  gbp: 300,  eur: 380}
    mac_pro:    {jpy: 250000, usd: 1800, gbp: 1500, eur: 1800}
    display:    {jpy: 50000,  usd: 380,  gbp: 300,  eur: 380}
  # How many promising listings to open fully each scan to hunt for a battery
  # cycle count in the description (slower, so capped).
  max_detail_fetch: 12
  max_detail_fetch_per_source: 3
  # Quick/partially blocked scans merge these last-good results instead of
  # making whole marketplaces disappear from the dashboard.
  snapshot_max_age_minutes: 360
  # Watch mode runs TWO cadences so you hear about deals fast without
  # hammering the slow browser-based sources:
  #   - a FULL scan (all sources) every watch_interval_minutes;
  #   - quick passes over just fast_sources every fast_interval_minutes
  #     in between (these are cheap HTTP/API scans, ~30-60s each).
  # Alerts fire per-source the moment each market is processed - a Mercari
  # find does not wait for Swappa's browser to finish.
  watch_interval_minutes: 20
  fast_interval_minutes: 5
  # Quick-pass sources: cheap HTTP/API scans only (the expanded query list
  # makes a full eBay site pass ~1-2 min, so the quick pass sticks to the
  # fastest high-churn markets; everything else refreshes on the full scan).
  fast_sources: [mercari, ebay_uk]

filters:
  # A listing containing any of these is skipped (junk / parts / box-only traps).
  exclude_keywords:
    - "ジャンク"          # junk
    - "修理交換用"        # repair-REPLACEMENT part (e.g. a screen, not the Mac)
    - "交換用"            # "for replacement" - parts/components
    - "修理用"            # for repair
    - "修理部品"          # repair part
    - "換装用"            # for swapping in (component)
    - "整備品"            # refurbished (seen in the wild as 整備品・未開封)
    - "再整備"            # re-furbished
    - "リファービッシュ"  # refurbished (katakana)
    - "部品取り"          # for parts
    - "部品"
    - "天板"              # top case / lid panel only
    - "液晶パネル"        # LCD panel (screen part)
    - "パネルのみ"        # panel only
    - "ディスプレイのみ"  # display only
    - "箱のみ"            # BOX ONLY - classic trap
    - "空箱"              # empty box
    - "外箱のみ"
    - "ケースのみ"        # case only
    - "充電器のみ"        # charger only
    - "ロジックボード"    # logic board (part)
    - "マザーボード"      # motherboard (part)
    - "トップケース"      # top case assembly (part)
    - "ボトムケース"      # bottom case (part)
    - "上半身"            # "upper half" = display assembly (parts trade term)
    - "液晶のみ"          # LCD only
    - "画面のみ"          # screen only
    - "アダプタのみ"
    - "画面割れ"          # cracked screen
    - "液晶不良"          # bad LCD
    - "キーボードのみ"
    - "ステッカー"        # stickers
    - "整備済"            # refurbished (not new/unused)
    # sold / reserved conventions JP sellers put in titles - the item can't
    # actually be bought even though it still shows as on sale
    - "予約済"            # reserved (pending buyer)
    - "売約済"            # sale already agreed
    - "取り置き"          # on hold for someone
    - "取置き"
    - "様専用"            # "reserved for Mr/Ms X" - Mercari reservation custom
    - "sold out"
    - "soldout"
    # English terms (mainly for eBay US/UK listings)
    - "junk"
    - "parts only"
    - "for parts"
    - "replacement"
    - "box only"
    - "empty box"
    # English part/accessory names (the JP equivalents are above) - these
    # are what "Genuine Apple ... Top Case / LCD Assembly" listings say
    - "top case"
    - "topcase"
    - "lcd"
    - "screen panel"
    - "screen assembly"
    - "display assembly"
    - "display panel"
    - "screen only"
    - "display only"
    - "display chassis"
    - "rear shell"
    - "rear housing"
    - "ssd expansion card"
    - "ssd expansion module"
    - "nvme enclosure"
    - "expansion tower"
    - "palmrest"
    - "palm rest"
    - "logic board"
    - "motherboard"
    - "bottom case"
    - "back cover"
    - "keyboard only"
    - "battery only"
    - "battery for"
    - "bezel"
    - "flex cable"
    - "hinge"
    - "for macbook"        # accessories: "Case/Charger/Skin for MacBook Pro"
    - "refurb"            # catches refurb/refurbished - not new/unused (also
                          # excluded by eBay's condition filter, but sellers
                          # mislist refurbs as "New" constantly)
    - "renewed"
    - "cert. ref"
    - "cracked"
    - "damaged"
    - "faulty"
    - "as-is"
    - "no shipping"       # local-pickup-only - can't reach a UK buyer
    - "local pickup only"
    - "activation lock"   # iCloud/MDM-locked machines are unusable
    - "icloud lock"
    - "mdm"
    - "demo unit"         # often DEP/MDM-enrolled corporate demo stock
    # Display accessory traps
    - "pencil only"
    - "ペンシルのみ"      # (Apple) Pencil only
    - "für macbook"
    - "stand only"
    - "スタンドのみ"      # stand only
    - "vesa adapter only"
    - "箱・スタンド"      # box + stand (no display)

# ------------------------------------------------------------------
# UK AVERAGE PRICES (GBP) for NEW / SEALED / OPENED-BUT-UNUSED units,
# base configuration of each model. These are starting estimates
# researched June 2026 - current-gen figures are anchored to real UK
# retail prices; discontinued models are estimates of the going rate
# for sealed/unused units on the UK secondhand market.
#
# Two ways to keep them accurate:
#   1) run:  python macdeals.py ukprices        (auto-suggests medians
#      from recent eBay UK SOLD listings, and  --write  saves them)
#   2) just edit the numbers below by hand.
# ------------------------------------------------------------------
models:
  - {id: m2pro-14,  chip: M2 PRO, size: 14, uk_avg_gbp: 1000, uk_used_gbp: 750, base_spec: "16GB/512GB",
     ebay_query: "MacBook Pro 14 M2 Pro -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m2pro-16,  chip: M2 PRO, size: 16, uk_avg_gbp: 912, uk_used_gbp: 735, base_spec: "16GB/512GB",
     ebay_query: "MacBook Pro 16 M2 Pro -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m2max-14,  chip: M2 MAX, size: 14, uk_avg_gbp: 1600, uk_used_gbp: 1231, base_spec: "32GB/1TB",
     ebay_query: "MacBook Pro 14 M2 Max -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m2max-16,  chip: M2 MAX, size: 16, uk_avg_gbp: 1799, uk_used_gbp: 1639, base_spec: "32GB/1TB",
     ebay_query: "MacBook Pro 16 M2 Max -case -cover -box -faulty -spares -repair -damaged"}

  - {id: m3-14,     chip: M3,     size: 14, uk_avg_gbp: 1050, base_spec: "8-16GB/512GB",
     ebay_query: "MacBook Pro 14 M3 -Pro -Max -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m3pro-14,  chip: M3 PRO, size: 14, uk_avg_gbp: 1078, uk_used_gbp: 823, base_spec: "18GB/512GB",
     ebay_query: "MacBook Pro 14 M3 Pro -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m3pro-16,  chip: M3 PRO, size: 16, uk_avg_gbp: 1800, uk_used_gbp: 1300, base_spec: "18GB/512GB",
     ebay_query: "MacBook Pro 16 M3 Pro -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m3max-14,  chip: M3 MAX, size: 14, uk_avg_gbp: 2200, uk_used_gbp: 1750, base_spec: "36GB/1TB",
     ebay_query: "MacBook Pro 14 M3 Max -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m3max-16,  chip: M3 MAX, size: 16, uk_avg_gbp: 2380, uk_used_gbp: 2046, base_spec: "36GB/1TB",
     ebay_query: "MacBook Pro 16 M3 Max -case -cover -box -faulty -spares -repair -damaged"}

  - {id: m4-14,     chip: M4,     size: 14, uk_avg_gbp: 1250, base_spec: "16GB/512GB",
     ebay_query: "MacBook Pro 14 M4 -Pro -Max -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m4pro-14,  chip: M4 PRO, size: 14, uk_avg_gbp: 1600, uk_used_gbp: 1299, base_spec: "24GB/512GB",
     ebay_query: "MacBook Pro 14 M4 Pro -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m4pro-16,  chip: M4 PRO, size: 16, uk_avg_gbp: 1842, uk_used_gbp: 1499, base_spec: "24GB/512GB",
     ebay_query: "MacBook Pro 16 M4 Pro -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m4max-14,  chip: M4 MAX, size: 14, uk_avg_gbp: 2230, uk_used_gbp: 2047, base_spec: "36GB/1TB",
     ebay_query: "MacBook Pro 14 M4 Max -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m4max-16,  chip: M4 MAX, size: 16, uk_avg_gbp: 2501, uk_used_gbp: 2800, base_spec: "36GB/1TB",
     ebay_query: "MacBook Pro 16 M4 Max -case -cover -box -faulty -spares -repair -damaged"}

  - {id: m5-14,     chip: M5,     size: 14, uk_avg_gbp: 1350, base_spec: "16GB/1TB",
     ebay_query: "MacBook Pro 14 M5 -Pro -Max -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m5pro-14,  chip: M5 PRO, size: 14, uk_avg_gbp: 1760, uk_used_gbp: 1180, base_spec: "24GB/1TB",
     ebay_query: "MacBook Pro 14 M5 Pro -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m5pro-16,  chip: M5 PRO, size: 16, uk_avg_gbp: 2148, uk_used_gbp: 1175, base_spec: "24GB/1TB",
     ebay_query: "MacBook Pro 16 M5 Pro -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m5max-14,  chip: M5 MAX, size: 14, uk_avg_gbp: 2899, uk_used_gbp: 2436, base_spec: "36GB/2TB",
     ebay_query: "MacBook Pro 14 M5 Max -case -cover -box -faulty -spares -repair -damaged"}
  - {id: m5max-16,  chip: M5 MAX, size: 16, uk_avg_gbp: 3100, base_spec: "36GB/2TB",
     ebay_query: "MacBook Pro 16 M5 Max -case -cover -box -faulty -spares -repair -damaged"}

  # ---- Mac mini (M2 Jan 2023, M4 Oct 2024) --------------------------------
  # uk_avg_gbp = what a near-new unit fetches on the UK market, July 2026
  # estimates seeded from RRP and typical secondhand levels - refresh with
  # `python macdeals.py ukprices --write` for real eBay-UK sold medians.
  - {id: mini-m2,     family: mac_mini, chip: M2,     uk_avg_gbp: 350, uk_used_gbp: 364,  base_spec: "8GB/256GB",
     ebay_query: "Mac mini M2 -Pro -case -mount -faulty -spares -repair -damaged"}
  - {id: mini-m2pro,  family: mac_mini, chip: M2 PRO, uk_avg_gbp: 687, uk_used_gbp: 580,  base_spec: "16GB/512GB",
     ebay_query: "Mac mini M2 Pro -case -mount -faulty -spares -repair -damaged"}
  - {id: mini-m4,     family: mac_mini, chip: M4,     uk_avg_gbp: 600, uk_used_gbp: 593,  base_spec: "16GB/256GB",
     ebay_query: "Mac mini M4 -Pro -case -mount -faulty -spares -repair -damaged"}
  - {id: mini-m4pro,  family: mac_mini, chip: M4 PRO, uk_avg_gbp: 1300, uk_used_gbp: 1277, base_spec: "24GB/512GB",
     ebay_query: "Mac mini M4 Pro -case -mount -faulty -spares -repair -damaged"}

  # ---- Mac Studio (M1 2022, M2 2023, M4 Max/M3 Ultra Mar 2025) ------------
  - {id: studio-m1max,   family: mac_studio, chip: M1 MAX,   uk_avg_gbp: 1000, uk_used_gbp: 996,  base_spec: "32GB/512GB",
     ebay_query: "Mac Studio M1 Max -faulty -spares -repair -damaged"}
  - {id: studio-m1ultra, family: mac_studio, chip: M1 ULTRA, uk_avg_gbp: 2200, uk_used_gbp: 2377, base_spec: "64GB/1TB",
     ebay_query: "Mac Studio M1 Ultra -faulty -spares -repair -damaged"}
  - {id: studio-m2max,   family: mac_studio, chip: M2 MAX,   uk_avg_gbp: 1350, uk_used_gbp: 1500, base_spec: "32GB/512GB",
     ebay_query: "Mac Studio M2 Max -faulty -spares -repair -damaged"}
  - {id: studio-m2ultra, family: mac_studio, chip: M2 ULTRA, uk_avg_gbp: 2452, uk_used_gbp: 3974, base_spec: "64GB/1TB",
     ebay_query: "Mac Studio M2 Ultra -faulty -spares -repair -damaged"}
  - {id: studio-m4max,   family: mac_studio, chip: M4 MAX,   uk_avg_gbp: 1900, uk_used_gbp: 1894, base_spec: "36GB/512GB",
     ebay_query: "Mac Studio M4 Max -faulty -spares -repair -damaged"}
  - {id: studio-m3ultra, family: mac_studio, chip: M3 ULTRA, uk_avg_gbp: 3800, uk_used_gbp: 3820, base_spec: "96GB/1TB",
     ebay_query: "Mac Studio M3 Ultra -faulty -spares -repair -damaged"}

  # ---- iMac 24" (M3 Oct 2023, M4 Oct 2024; M1 is 2021 = out of scope) -----
  - {id: imac-m3, family: imac, chip: M3, size: 24, uk_avg_gbp: 1000, uk_used_gbp: 682,  base_spec: "8GB/256GB",
     ebay_query: "iMac 24 M3 -faulty -spares -repair -damaged -cracked"}
  - {id: imac-m4, family: imac, chip: M4, size: 24, uk_avg_gbp: 1100, uk_used_gbp: 885, base_spec: "16GB/256GB",
     ebay_query: "iMac 24 M4 -faulty -spares -repair -damaged -cracked"}

  # ---- Mac Pro (M2 Ultra, June 2023 - thin market, big ticket) ------------
  - {id: macpro-m2ultra, family: mac_pro, chip: M2 ULTRA, uk_avg_gbp: 3900, base_spec: "64GB/1TB",
     ebay_query: "Mac Pro M2 Ultra -rack -faulty -spares -repair -damaged"}

  # ---- Apple Studio Display (Mar 2022; standard glass/tilt stand base) ----
  # Nano-texture glass and the height-adjustable stand fetch £150-350 more -
  # treat a cheap one of those as a bonus, the benchmark is the base model.
  - {id: studio-display, family: display, uk_avg_gbp: 975, base_spec: "27-inch",
     ebay_query: "Apple Studio Display -mount -arm -stand only -faulty -cracked -spares -repair"}
